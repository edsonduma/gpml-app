import 'package:flutter/material.dart';

abstract class AbstractScreenModel extends StatelessWidget {
  const AbstractScreenModel({super.key});

  set setModel(final dataModel);

  setModel2(final dataModel);
}
